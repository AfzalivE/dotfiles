# aliases
alias rm='rm -i'
alias gdw='./gradlew'
alias gdw-refresh='gdw --refresh-dependencies'

export NVM_DIR="/Users/afzal/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" --no-use  # This loads nvmeval $(/usr/libexec/path_helper -s)
